spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/hospital_management_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:1234}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
app:
  superadmin:
    name: ${SUPERADMIN_NAME:Diskahnta Acharya}
    username: ${SUPERADMIN_USERNAME:dikshanta8080}
    password: ${SUPERADMIN_PASSWORD:root}
    email: ${SUPERADMIN_EMAIL:dikshantaacharya04@gmail.com}
  jwt:
    secret: ${JWT_SECRET:bXlTdXBlclNlY3JldEtleVRoYXRJc0xvbmdFbm91Z2hGb3JITVNTNTI=}
    expiry: ${JWT_EXPIRY:10}
